var assert = require("assert");
var utils = require("../utils");

describe('utils', function() {
    describe('rmDir', function() {
        it('should remove given directory', function() {
            assert.equal(1, 1, 'Dummy test to get travis working');
        });
    });
});