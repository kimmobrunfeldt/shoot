// Default configuration

module.exports = {
    // Crawl mode
    crawl: false,
    // Which resolutions should we shoot
    resolutions: ['1366x768'],
    // Output directory for screenshots
    outputDir: '.shoot',
    // Force mode will delete files and directories without prompting
    force: false,
    crawler: 'StaticCrawler'
};